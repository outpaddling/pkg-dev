#!/bin/sh -e

if [ $# -gt 1 ]; then
    printf "Usage: $0 [dir]\n"
    exit 1
fi

if [ `uname` = 'NetBSD' ]; then
    make='make'
else
    make='bmake'
fi

if [ $# = 1 ]; then
    cd $1
fi
$make deinstall
$make distclean
$make install

